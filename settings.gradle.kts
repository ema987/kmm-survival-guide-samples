pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kmm-survival-guide-samples"
include(":androidApp")
include(":shared")